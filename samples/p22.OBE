module Programa9;
var a, b, i : integer;

begin

   write("Inicial: "); 
   read(a);

   write("Final: "); 
   read(b);

   for i := a To b Do
       write("i: ");
       writeln(i);
   end;
	
end Programa9.
